import ParentDashboard from "@/components/dashboard/parent-dahboard";

/**
 * §5.3 — Parent dashboard.
 *
 * The <DashboardShell> is provided by <ParentLayout> — this page just
 * renders the dashboard content.
 */
export default async function DashboardPage() {
  return <ParentDashboard />;
}
